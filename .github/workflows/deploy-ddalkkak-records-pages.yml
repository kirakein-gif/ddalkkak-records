name: Deploy migration test to GitHub Pages (manual legacy)

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic Pages deployment now uses the repository Pages build. This legacy workflow is manual-only."
