# migration

## 기존 기록물관리대장 표준전환

지역별로 파편화된 과거 기록물관리대장을 충청남도교육청 표준양식으로 변환하는 웹 도구.

현재 HTML 프로토타입에서 검증된 핵심 개념:
- 학교/직속/기록관 표준서식 선택
- 원본 대장 헤더 자동 탐지
- 표준 필드 자동 매칭
- 드래그 방식 수동 보정
- JSON 매핑 저장
- 표준 Excel 출력

향후에는 JSON을 단순 열 매핑이 아니라 값 변환·분해·결합·검증까지 포함한 '지역 변환 프로필'로 확장한다.
